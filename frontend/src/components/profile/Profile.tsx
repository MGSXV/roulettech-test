const Profile = () => {

	return (
		<div className={`w-full min-h-[80vh] flex justify-center py-28 bg-primary`}>
			<div className={`w-full max-w-7xl flex flex-col items-center justify-center`}>
				<h1 className={`text-white text-2xl font-title uppercase text-center`}>
					you are logged in
				</h1>
				<h2 className={`text-white text-lg font-title text-center`}>
					We can do incredible things together
				</h2>
			</div>
		</div>
	)
}

export default Profile;